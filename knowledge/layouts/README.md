# Production layout artifacts

This directory is the boundary for future reviewed `LayoutArtifact` files validated by `knowledge/schemas/layout-artifact.schema.json`. WP-02 does not create a production ELK layout; validation-spike layouts remain isolated under the spike implementation.
